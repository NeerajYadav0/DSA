int isPalindrome(String s) {
       int j=s.length()-1;
       for(int i=0;i<s.length()/2;i++,j--)
       {
           if(s.charAt(i)!=s.charAt(j))
           {
               return 0;
           }
       }
       return 1;
    }
